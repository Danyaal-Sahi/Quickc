# quickq

`quickq` is a tiny terminal search command for coding queries.

## Setup

```bash
./bin/install-quickq
source ~/.zshrc
```

## Usage

```bash
quickq "latest version of nodejs"
quickq "python package for working with aws"
```
